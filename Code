<!DOCTYPE html>
<html>
<body>

<?php
// PHP code (PHP statements end with a semicolon)
# Also a comment
$gender = "male";  // variable starts with $ sign
$n = 10;
$f = 4.5;
$b = true;
echo "I am $gender";  // outputs I am male
define("pi", 3.14159)  // assigns pi as a constant with the value 3.14159
$oceans = array("Pacific", "Atlantic", "Indian", "Arctic");  // creates an array with four terms
sort($oceans);  // sorts alphabetically (i.e. "Arctic", "Atlantic", "Indian", "Pacific")
rsort($oceans);  // sorts reverse alphabetically (i.e. "Pacific", "Indian", "Atlantic", "Arctic")

if (condition) {
  // code
} elseif (condition) {
  // code
} else {
  // code
}

switch (n) {
    case label1:
        code to be executed if n=label1;
        break;
    case label2:
        code to be executed if n=label2;
        break;
    ...
    default:
        code to be executed if n is different than all labels;
}

while (condition is true) {
    code to be executed;
}

for (init counter; test counter; increment counter) {
    code to be executed;
}
foreach ($array as $value) {
    code to be executed;
}

function functionName(<arguments>) {
    code to be executed;
    return <variable>;
}

?>

</body>
</html>
